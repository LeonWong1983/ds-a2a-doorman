# ── Internal-only material (never publish) ────────────────────────────────────
# Raw reverse-engineering references, session-log dumps, and internal working
# notes contain machine-specific paths, live credentials and third-party
# business data. They stay local.
docs/_ref/
docs/HANDOFF.md
docs/probe-log.md
docs/cordis-round-report.md
docs/START-cordis-session.md
# Internal design drafts (phase-D forwarder + rollout plan). The publishable
# architecture write-up lives in docs/DESIGN.md.
plugin/

# Decoded / captured session transcripts in any location
*.jsonl
*.jsonl.zstd
decoded-*

# ── Local secrets ────────────────────────────────────────────────────────────
.env
.env.*
*.local.yml
secrets/

# ── Node / tooling ───────────────────────────────────────────────────────────
node_modules/
pnpm-lock.yaml
package-lock.json
.npm/

# ── OS / editor noise ────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.idea/
.vscode/
